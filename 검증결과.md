# JavaScript HWPX 생성 검증

검증일: 2026-09-14

## 실행 결과

`generate-hwpx.js`를 Node.js로 실행해 `풀무원_사용신청서_검토용.hwpx`와 `풀무원_활용계획서_검토용.hwpx`를 생성했습니다. 원본 `.hwpx`의 본문 XML을 실제로 바꾸고 새 ZIP 패키지를 만들었습니다. AI API나 한글 오토메이션을 호출하지 않았습니다.

현재 노션 입력값 중 상품 `두부 / DOOBU`까지 반영했습니다. 미입력 값은 확인 필요로 표시했습니다. 데이터의 사실성·실제/가상 구분은 검증하지 않았습니다.

## 구조·데이터 검사

| 항목 | 결과 |
|---|---|
| 사용신청서 지정 42개 셀 값 대조 | 통과 |
| 활용계획서 지정 9개 셀 값 대조 | 통과 |
| 각 파일 내부 XML 8개 문법 검사 | 통과 |
| ZIP CRC·재열기 | 통과 |
| mimetype 첫 항목·무압축 | 통과 |
| 글자·문단 스타일 및 manifest 참조 | 통과 |
| 수정 대상 외 패키지 파일 바이트 보존 | 통과 |
| 사용자 원본 2개 SHA-256 보존 | 통과 |

## 별도 회귀시험

JavaScript 생성 결과를 Python `zipfile`과 `ElementTree`로 다시 읽어 독립 대조했습니다.

- `&`, `<`, `>`, 따옴표, 이모지, 여러 문단 줄바꿈 유지: 통과
- 국가 3개·공장 4개 경계값 매핑: 통과
- 공백뿐인 필수값을 확인 필요로 처리: 통과
- 상품 2개, 국가 4개, 미지원 사진 경로, 변경된 원본 양식 거부: 통과
- 오류 작업이 결과 문서를 만들지 않는지 확인: 통과

## 실제 한컴 표시

생성된 두 파일을 한컴 뷰어에서 직접 열어 보는 검사는 완료하지 못했습니다. 설치된 한컴 뷰어의 화면 조회가 중단되어, 생성 파일의 열기 성공·쪽수·페이지 배치는 미검증입니다. 위의 통과 결과는 구조·데이터 검사에 한정됩니다.

## 남은 범위

사진 삽입, 여러 상품 세부정보 자동 반복, 자동 PDF 변환은 미구현입니다. 새로운 길이의 문안과 여러 국가를 실제로 사용할 때는 표 잘림·줄바꿈·쪽수를 다시 확인해야 합니다. 제출용 사실 확인과 동의·날인은 고객사가 진행해야 합니다.
